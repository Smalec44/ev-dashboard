/**
 * How the app introduces itself to the services it calls. Several of them —
 * the FOSSGIS router, the news feeds — ask for a User-Agent that names the
 * application and says where to find it, not a library default.
 */
export const USER_AGENT = "ev-dashboard/0.1 (+https://github.com/Smalec44/ev-dashboard)";
